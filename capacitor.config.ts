import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'edu.uw.cirg.wahealthsummary',
  appName: 'WA Health Summary',
  webDir: 'build'
};

export default config;
